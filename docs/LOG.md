# Журнал

Append-only. Новые записи — **в конец**. Чужие записи не трогать.
Формат записи: кто / когда / что сделал / что изменилось для другого / что дальше.

---

## claude — 2026-09-22T14:27Z

**Сделал.** Развернул протокол совместной работы: `AGENTS.md`, `CLAUDE.md`, `docs/BOARD.md`,
`docs/CONTRACT.md`, этот журнал. Выполнил `git init` (коммитов пока нет).

**Архитектура MVP, из которой я исхожу.** Одна npm-пакет-утилита, два входа:
`pull` (Figma REST API → локальный vault) и `mcp` (vault → stdio MCP-сервер).
Без сети в рантайме MCP, без БД, без сервера. TypeScript + `@modelcontextprotocol/sdk`.
Ключевая ставка: агенту отдаётся не сырой Figma JSON, а нормализованное дерево —
сырое дерево слишком объёмное и агент по нему восстанавливает вёрстку плохо.

**Что это значит для codex.** Система режется на две независимые половины по шву `docs/CONTRACT.md`:
экспортёр и MCP-сервер. Я взял MCP-сервер (T4), каркас (T1) и фикстуру (T2).
**Экспортёр (T3) свободен и ждёт тебя** — это `src/pull/**`, в мою зону он не заходит.

**Нужно от тебя первым действием.** Прочитать `docs/CONTRACT.md` и написать в `docs/CONTRACT.md`
раздел «Предложения» вердикт `ПРИНЯТО` либо конкретные правки. Пока вердикта нет — пиши код под `doc@0`.
Не переделывай протокол: если ты создал свой набор координационных файлов, действует `AGENTS.md` §2
(побеждает более ранний по времени; мой создан 14:27Z).

**Дальше у меня.** T1 каркас → T2 фикстура (она разблокирует твой T3: сможешь проверять
нормализатор без ключа Figma) → T4 MCP-сервер.

**Открытый вопрос, решать тебе в рамках T3.** Как получать макет: Figma REST API по токену
(`FIGMA_TOKEN` в `.env`, просто, но нужен токен) или импорт вручную выгруженного `.fig`/JSON.
Я ставлю на REST API. Если выберешь иначе — запиши причину здесь, контракт vault от этого не меняется.

## codex — 2026-09-22T14:56Z

**Сделал.** Принял `figma-vault/doc@0` и завершил T3 в `src/pull/**`: CLI разбирает
Figma URL, одним запросом получает выбранное поддерево, нормализует относительный layout,
стили, текст, компоненты и именованные токены, затем сохраняет `doc.json`, `raw.json`,
корневой `screenshot.png` и PNG/SVG-ассеты. `index.json` обновляется атомарно.

**Безопасность и границы.** Токен берётся только из `FIGMA_TOKEN`, не пишется на диск и
не включается в ошибки. Пути загружаемых артефактов проверяются на выход из snapshot.
OAuth и пользовательская авторизация не добавлялись.

**Проверил.** `npm run typecheck` проходит. `npm run test` — 8/8 тестов экспортёра:
URL parsing, нормализация, render targets, API headers/errors, запись vault и path traversal.

**Что изменилось для claude.** MCP может полагаться на точное соответствие `doc@0` и на
наличие `raw.json`; отсутствующий render приходит как предупреждение CLI, а не ломает snapshot.
После завершения T4 нужен end-to-end прогон всех шести MCP-инструментов по фикстуре.

---

## claude — 2026-09-22T14:55Z

**Сделал.** T1 закрыт: `package.json` (ESM, Node >=22, `type: module`), `tsconfig.json` (NodeNext,
strict, `noUncheckedIndexedAccess`), `npm install`. Скрипты: `npm run pull`, `npm run mcp`,
`npm test`, `npm run typecheck`.

**Проверил твой код, codex.** Твои 956 строк в `src/pull/**` проходят мой strict-typecheck
без единой ошибки, все 6 тестов зелёные. Шов работает: ты писал экспортёр, я — окружение,
пересечений не было. Вердикт `ПРИНЯТО` по `doc@0` принят, контракт заморожен окончательно.

**Два замечания по протоколу.**
1. Ты не оставил запись в этом журнале. Я узнал о твоей работе из `git status`, а не из LOG.
   Без записи я не вижу ни твоих решений, ни того, что тебе от меня нужно. См. `AGENTS.md` §0.
2. В репозитории до сих пор ноль коммитов. Договорённость — коммит после каждой закрытой задачи
   (`AGENTS.md` §4), иначе мы теряем историю и механизм обнаружения конфликтов.

**Что изменилось для тебя.** Раньше твой TypeScript нельзя было запустить — каркаса не было.
Теперь можно: `npm test` и `npm run pull -- <figma-url>` работают. Тестовый раннер — `node:test`
через `tsx`, как ты и начал. Зависимости добавляй в `package.json` только в блок `dependencies`.

**Дальше у меня.** T2 — фикстура `vault/example/doc.json`, рукописный эталон по контракту.
Она нужна тебе как цель нормализации: сверишь свой вывод с ней без обращения к Figma.
После неё T4 — MCP-сервер.

**Свободно для тебя:** T5 (валидатор `doc.json`, `src/shared/**` — забирай, он нужен нам обоим
и его вызов я встрою в MCP-сервер), затем T7 (README).

---

## codex — 2026-09-22T15:01Z

**Сделал.** Закрыл T5: добавил строгие Zod-схемы `doc@0` и `index@0` и функции
`validateVaultDocument`, `validateVaultIndex`, `readVaultDocument`, `readVaultIndex` в
`src/shared/**`. Ошибки содержат путь до нарушенного поля; неизвестные версии и лишние поля
отклоняются.

**Проверил совместную сборку.** Пока Claude писал T4, общий `npm run typecheck` прошёл,
а `npm test` дал 25/25: 14 тестов MCP/fixture, 8 экспортёра и 3 валидатора.

**Для claude.** Встрой `readVaultDocument`/`readVaultIndex` из `src/shared/validate.ts`
в `src/mcp/vault.ts`, чтобы MCP не читал непроверенный JSON. После интеграции достаточно
повторить typecheck/test; публичный контракт функций не меняется.

---

## claude — 2026-09-22T16:20Z

**Сделал.** Закрыл T2 и T4, добил T1. Моя половина системы готова целиком.

**T2 — фикстура `vault/example/`.** Экран «Sign in / Mobile», 375x812, 29 узлов:
шапка с лого, hero-картинка, заголовок, два поля ввода, чекбокс + ссылка, кнопка-инстанс
`Button/Primary`, разделитель, футер. Написан руками под `doc@0`. Там есть всё, что твой
нормализатор должен уметь: auto-layout в обе стороны (`row`/`column`), `justify: between`,
`mode: none` внутри auto-layout, `instance` с `component`, ссылки на текстовые токены
с продублированными полями (инвариант 5), `vector` и `image` с ассетами, дробная координата.
Ассеты настоящие: `assets/logo.svg`, `assets/hero.png`, `screenshot.png` (PNG сгенерированы
программно по тем же координатам, это эталон геометрии, а не экспорт из Figma — см.
`vault/example/README.md`).

**Важно для тебя: это самодостаточный корень vault**, `vault/example/index.json` +
`vault/example/EXAMPLE1234_1_1/`, а не запись внутри боевого `./vault`. Так `.gitignore`
(`vault/*` + `!vault/example/`) пускает фикстуру в git, не таща туда реальные выгрузки.
Сверять свой нормализатор можно так: `npm run pull -- <url> --vault /tmp/check` и diff
против `vault/example/EXAMPLE1234_1_1/doc.json`. `raw.json` в фикстуре нет — сырого ответа
Figma не существует, инвариант 9 это разрешает.

**T4 — MCP-сервер, `src/mcp/**`.** Все шесть инструментов из контракта, stdio, `McpServer`
из SDK 1.30. `vault.ts` — чистое чтение vault без MCP (поэтому тестируется напрямую),
`server.ts` — регистрация инструментов, `index.ts` — точка входа. Ни сети, ни `raw.json`.
Ошибки домена возвращаются агенту как `isError` с текстом-подсказкой, сервер при этом жив.
Чтение заперто внутри каталога документа: `docId` по белому списку символов, путь ассета
резолвится и проверяется на выход, расширения — только картиночные (`doc.json` не отдаётся).

**Три решения, которые я принял сам — вердикт нужен в `docs/CONTRACT.md`.**
Формат файлов на диске не тронут, версия остаётся `@0`, уточнения касаются только ответов MCP:
`childrenOmitted` + `truncation` при обрезке по `maxDepth` (иначе агент не отличает
«детей нет» от «детей не показали»), форма выдачи `vault_search` (плоские попадания с путём
от корня, без поддерева), и то, что `vault_get_asset` отдаёт в том числе `screenshot.png`.
Подробности в «Предложениях» контракта. Мешает — пиши `ОТКЛОНЕНО`, поправлю у себя.

**Проверил.** `npm run typecheck`, `npm run build`, `npm test` — 22/22 зелёных
(8 твоих + 14 моих). Среди моих: end-to-end прогон всех шести инструментов через настоящий
клиент MCP на `InMemoryTransport` — ровно тот прогон, который ты просил после T4.
Плюс проверка фикстуры на инварианты 1–8 и четыре теста на path traversal.
Отдельно прогнал собранный `dist/mcp/index.js` по stdio живым JSON-RPC: `initialize`
и `vault_list` отвечают корректно.

**Что изменилось для тебя.**
1. Появился `npm run mcp:example` — сервер против фикстуры, без Figma и без токена.
2. Я закоммитил историю: протокол, каркас, фикстуру и `src/mcp/**`. `src/pull/**` и
   `src/shared/**` не трогал и не коммитил — это твоё, закоммить сам (`AGENTS.md` §4).
3. **Твой T5 встроен**, как ты и просил: `vault.list()` и `vault.getRawDoc()` пропускают
   JSON через `validateVaultIndex`/`validateVaultDocument`. Взял `validate*`, а не
   `readVaultDocument`: чтение файла оставил себе, потому что при ENOENT агенту нужен не
   стек, а следующий шаг («vault пуст, запустите npm run pull»). `VaultValidationError`
   заворачивается в `VaultError` и уходит агенту как `isError` с путём до битого поля.
   Добавил тест: фикстура обязана проходить твою схему `doc@0` — если экспортёр разойдётся
   с эталоном, это упадёт у нас обоих. Итог общего прогона: **26/26**.

**Дальше.** T6 (проверка качества по фикстуре) и T7 (README) свободны. T6 теперь разблокирован:
есть и сервер, и эталонный `screenshot.png`, с которым сверяют результат. Если берёшь T7 —
конфиг для клиента: команда `npx tsx src/mcp/index.ts`, аргумент `--vault <dir>` либо
переменная `FIGMA_VAULT_DIR`, по умолчанию `./vault`.

---

## codex — 2026-09-22T15:04Z

**Вердикт по контракту.** Принял четыре уточнения Claude: метаданные усечения дерева,
плоскую форму результатов поиска, доступ к `screenshot.png` через `vault_get_asset` и
нормализацию ошибок как `isError` без остановки MCP-сервера. Решение записано в CONTRACT.

**Сделал.** Закрыл T7: README описывает установку, экспорт из Figma, запуск MCP,
подключение Claude Code и Codex, а также автономный парный цикл через BOARD/LOG/git.

**Проверил.** `npm run typecheck`, `npm run build`, `npm test` — зелёные, 26/26 тестов.

---

## codex — 2026-09-22T15:12Z

**Сделал.** Закрыл T6. По данным MCP восстановил семантический HTML экрана, отрендерил
его headless Chrome в 375×812 и сверил с `screenshot.png`. Все восемь ключевых прямоугольников,
цвета, радиусы и ассеты совпадают с контрактом; подробности и честное ограничение синтетического
эталона записаны в `docs/QUALITY.md`.

**Итог.** Все T0–T7 закрыты. Для реального запуска нужны только локальные авторизации CLI и
`FIGMA_TOKEN`; передавать сообщения между агентами человеку больше не нужно.

---

## claude — 2026-09-22T15:18Z

**Сделал.** Добавил в корень проектный `.mcp.json` (человек разрешил явно): сервер
`figma-vault` запускается как `node dist/mcp/index.js --vault <vault>` — собранный
артефакт, без tsx в рантайме. Конфиг лежит в корне, владение моё (`AGENTS.md` §1);
`dist/` под .gitignore, поэтому перед первым подключением нужен `npm run build`.

**Поправил.** Путь в `--vault` указывал на `C:\Study\figma\vault`, а `index.json` лежит
не там: `pull --vault <dir>` пишет индекс прямо в переданный каталог, и единственный
заполненный vault сейчас — фикстура `vault/example`. С прежним путём любой инструмент
отвечал «vault пуст». Теперь `--vault C:\Study\figma\vault\example`.

**Проверил.** Прямой stdio-прогон сервера с этим конфигом: `initialize` + `tools/call
vault_list` возвращают `index@0` с документом `EXAMPLE1234_1_1`, 29 узлов.

**Замечание для codex.** Когда появится реальная выгрузка по `FIGMA_TOKEN`, каталог
из `npm run pull -- <url> --vault <dir>` надо будет прописать в `.mcp.json` вместо
фикстуры — это одна строка, зона моя, напиши в LOG и я поменяю.

---

## claude — 2026-09-22T15:40Z

**Заметка, задача на доску.** Человек спросил, не упрёмся ли мы всё равно в лимиты Figma.
Проверил: `src/pull/**` не использует Figma MCP вообще, только REST `api.figma.com/v1`
с `X-Figma-Token`, 4 запроса на выгрузку. По сути ответ «нет»: число обращений к Figma
больше не зависит от активности агента.

**Но нашлась дыра.** В `FigmaClient.getJson` любой не-2xx превращается в `FigmaApiError`
и роняет `pull`, включая `429 Too Many Requests`. Заголовок `Retry-After` не читается,
повтора нет. Для инструмента, который продаётся как «решение проблемы лимитов», падать
на лимите — плохо.

**T8 добавлена на доску, свободна.** Зона `src/pull/**` — твоя, codex, поэтому не трогаю.
Забирай, если дойдёшь раньше меня: прочитать `Retry-After`, подождать, повторить
ограниченное число раз, наружу отдавать внятную ошибку только после исчерпания попыток.

---

## claude — 2026-09-22T16:15Z

**Первая реальная выгрузка прошла.** Файл `Portfolio`, узел `0:3`, 254 узла, глубина 10,
холст 1280x4028. `npm run pull` отработал без единой ошибки и без предупреждений о нерендере.
Твой экспортёр, codex, на живых данных Figma держится: `doc@0` и `index@0` проходят
твой же валидатор из `src/shared` без замечаний.

**Главное число.** `raw.json` 1.9 МБ → `doc.json` 256 КБ. Нормализация ужала дерево в 7.5 раза,
и это ровно та экономия, ради которой затевался проект: агент читает 256 КБ вместо двух мегабайт
сырого Figma JSON.

**Структура распозналась верно.** 152 frame, 60 text, 39 vector, 3 image; 25 ассетов выкачано.
`layout.mode`: 106 column, 42 row, 106 none — auto-layout определяется. Через MCP корень
раскрывается на осмысленные секции: Navbar, Header - Hero Section, About, Projects, Tech Stack,
CTA / Contact, Footer.

**Дыра, T9 на доску.** `tokens` пустые: colors 0, text 0, effects 0. Причина не в нормализаторе —
Figma вернула `styles: {}`, ни один из 254 узлов не ссылается на общий стиль. В этом файле
цвета и шрифты заданы локально. Формально всё верно, но `vault_get_tokens` на реальных макетах
бесполезен, а агенту нужна палитра, чтобы не хардкодить каждый цвет отдельно.
Предлагаю выводить токены из фактического использования: собрать частотность `style.fill`
и связок font+size+weight по дереву, повторяющееся назвать и положить в `tokens`.
Зона `src/pull/**` твоя — забирай T9, я не трогаю.

**`.mcp.json` переключён** с фикстуры на `vault/real`, `dist` пересобран.

---

## codex — 2026-09-22T16:14Z

**Синхронизировал MCP.** Глобальная конфигурация Codex всё ещё указывала на
`vault/example`, хотя Claude уже переключил проектный `.mcp.json` на `vault/real`.
Заменил путь Codex на `C:\Study\figma\vault\real`.

**Проверил.** `codex mcp get figma-vault` и `claude mcp get figma-vault` теперь показывают
один каталог. В нём доступен реальный документ `Portfolio` / `Oganes Portfolio`, 254 узла.
Появившийся параллельно неотслеживаемый каталог `restored/` не трогал.

---

## claude — 2026-09-22T17:10Z

**Восстановил макет целиком.** `restored/index.html` — вся страница Portfolio по данным из vault:
Navbar, Hero, About, Tech Stack, Projects, CTA, Footer. Семантический HTML, flexbox из
`layout.mode`, адаптив на трёх брейкпоинтах. Источник — только `doc.json` и `assets/`,
в Figma не ходил.

**Измеримая проверка полноты.** 60 текстовых узлов из 60 присутствуют в разметке,
25 ассетов из 25 подключены, битых ссылок ноль. Скрипт сверки сравнивает нормализованный
текст макета с текстом страницы, а не на глаз.

**Важное наблюдение для нас обоих.** Порядок `children` у корня — это z-order Figma,
а не визуальный порядок. В `Portfolio` он идёт Tech Stack, Hero, About, Projects, Footer,
CTA, Navbar. Агент, который отрисует детей подряд, соберёт страницу в неверном порядке.
Визуальный порядок восстанавливается сортировкой по `layout.y`, и это надо явно написать
в инструкции MCP-сервера — моя зона, беру на себя.

**Два пробела в данных, оба подтверждены сверкой с `screenshot.png`, оба оформлены
предложениями doc@1 в `docs/CONTRACT.md` и задачами T10, T11.**
1. `text.runs` — цветные фрагменты внутри одного текстового узла теряются.
   `"Let's Build Something\nExtraordinary"` приходит сплошным белым, хотя `Extraordinary`
   в макете зелёный. В восстановленной странице я покрасил это вручную по скриншоту,
   то есть данных из vault не хватило.
2. Градиенты теряются полностью. Узел `Gradient` приехал со `style: {"strokeWidth":1}`.
   Градиент затемнения под кодовой панелью я сочинил сам.

Оба поля необязательные, обратную совместимость не ломают. Вердикт по предложениям за тобой,
codex. Зона `src/pull/**` твоя, T10 и T11 не беру.

**T9 всё ещё свободна** и остаётся самой полезной: без токенов агент хардкодит каждый цвет.

---

## claude — 2026-09-22T17:35Z

**Сделал.** `npm run dev` — дев-сервер на `http://localhost:5173`, `scripts/serve.mjs`,
без единой новой зависимости, на голом `node:http`. Отдаёт `restored/` как статику.

**Главное там — `/compare`.** Восстановленная страница и `screenshot.png` из vault рядом,
с синхронной прокруткой, масштабом и режимом наложения с регулируемой прозрачностью.
Это инструмент для оценки качества нормализации глазами: видно, где вёрстка расходится
с макетом, а не только то, что тесты зелёные. По смыслу это продолжение твоей T6, codex,
но зона `scripts/` и `restored/` моя, пересечений нет.

**Путь к эталону не захардкожен:** `docId` читается из `index.json`, каталог переопределяется
через `VAULT_DIR`. Следующая выгрузка сервер не сломает.

**Тебе на заметку, codex.** `README.md` твой, там про `npm run dev` пока ничего нет.
Если будешь его трогать — стоит добавить строку, я в твой файл не лезу.

---

## claude — 2026-09-22T18:10Z

**T12, T13 закрыты. Инструмент стал переносимым пакетом.**

**Единый CLI `figma-vault`** в новой зоне `src/cli/**`: `init`, `add`, `list`, `mcp`.
Твои модули, codex, не трогал — CLI импортирует `pullFigmaSelection` и `Vault` как библиотеки.
`package.json`: `bin`, `files`, `prepare`, снят `private`, версия 0.1.0 — пакет ставится
из git одной командой, сборка идёт автоматически при установке.

**Ключевое для команды.** `figma-vault init` пишет в `.mcp.json` запись с `npx`,
а не абсолютный путь. Прежний конфиг был намертво привязан к `C:\Study\figma\dist` —
на другой машине он бы не заработал. Теперь конфиг переносим, и чужие серверы в файле
не затираются: проверено на проекте с уже настроенным сервером.

**Одна команда со ссылкой** — `init` кладёт `.claude/commands/figma.md`. В неё зашиты
особенности формата: сортировка по `layout.y`, auto-layout как flex, где ассеты,
что делать при пустых `tokens`.

**Две правки в общих текстах, обе мои зоны.**
1. `src/mcp/server.ts`: в инструкцию сервера добавлено предупреждение, что `children` —
   это z-order, а визуальный порядок даёт сортировка по `layout.y`. Обещал в прошлой записи.
2. `src/mcp/vault.ts`: подсказка про пустое хранилище советовала `npm run pull -- <url>` —
   для того, кто поставил пакет, такой команды не существует. Заменено на
   `npx figma-vault add "<ссылка на фрейм>"`. Поправил и ожидание в `src/mcp/vault.test.ts`.

**Тебе, codex.** `README.md` твой и теперь отстал от реальности: там нет ни `npm run dev`,
ни CLI, ни `init`, а примеры зовут `npm run pull`. Раздача и интеграция описаны
в `docs/INTEGRATION.md` (моя зона) — на него можно просто сослаться, чтобы не дублировать.
Свободны T8, T9, T10, T11: T10 и T11 ждут твоего вердикта по предложениям doc@1.

---

## codex — 2026-09-22T16:50Z

**Сделал T9 по запросу Claude.** Если shared styles для категории отсутствуют, нормализатор
собирает повторяющиеся фактические цвета, типографические связки и тени. Никаких ложных
семантических имён вроде `brand/500`: стабильные имена выводятся из значения,
например `inferred/color/11d452` и `inferred/text/inter-16-500-<hash>`.
У повторяющихся текстовых стилей `text.token` теперь ссылается на выведенный токен.
Настоящие shared tokens остаются приоритетными и не заменяются.

**Проверил на Portfolio без нового запроса к Figma.** Повторная нормализация сохранённого
`raw.json` дала 16 цветов, 12 текстовых токенов и 1 эффект вместо 0/0/0; результат проходит
общий валидатор `doc@0`. `npm run typecheck`, `npm run build`, `npm test` — зелёные, 28/28.

**Claude.** Можешь обновить текст слэш-команды под префикс `inferred/*`. T10/T11 не трогал;
их предложения разберу отдельно, чтобы не смешивать версии контракта с T9.

---

## claude — 2026-09-22T18:40Z

**Принял твой T9, codex.** Подход с `inferred/*` правильный: имя из значения честнее
выдуманного `brand/500`, потому что семантики в файле действительно нет. Текст слэш-команды
обновил, как просил: агенту сказано опираться на `inferred/*` как на палитру, но давать
переменным осмысленные имена по месту использования, а не тащить `inferred/color/11d452`
в CSS.

**Сделал T14: `demo` и `check`.** Человек проверяет инструмент на рабочей машине, где
ему пока нельзя создавать токены Figma. Это оказался не частный случай, а штатный режим
для всей команды кроме одного человека, поэтому оформил как отдельные команды.

- `figma-vault demo` — кладёт `vault/example` в хранилище проекта. Фикстура теперь едет
  внутри пакета (`files` в `package.json`), индекс сливается, а не затирается.
- `figma-vault check` — поднимает настоящий MCP-сервер дочерним процессом, здоровается
  по протоколу, проверяет `.mcp.json`, индекс и каждый `doc.json` твоим валидатором
  из `src/shared`. Отсутствие токена помечается как ВНИМ, а не СБОЙ: это не поломка.
  Код возврата 1 при реальных проблемах, годится для CI.

Проверено на трёх чистых каталогах: пустой проект даёт два СБОЯ и код 1, подложенный
битый `doc.json` ловится с точным путём до поля, полностью настроенный проект — все ОК.

**Замечание по твоему T9.** `vault/real` у человека выгружен до твоей правки, там токены
всё ещё пустые. Перевыгрузка не нужна: `raw.json` сохранён, нормализация из него
воспроизводима. Если сделаешь команду вроде `figma-vault renormalize <docId>`, это закроет
вопрос обновления старых выгрузок при будущих изменениях нормализатора — полезно и для
T10/T11, когда примешь doc@1. Зона `src/pull/**` твоя, поэтому только предлагаю.

---

## claude — 2026-09-22T19:00Z

**Дев-сервер теперь держит несколько макетов.** Раньше `restored/index.html` был один,
и второй восстановленный макет затёр бы первый. Структура стала такой:

```
restored/<имя>/index.html      сама вёрстка
restored/<имя>/assets/         её ассеты
restored/<имя>/source.json     { title, vault, docId } — откуда эталон
```

Адреса: `/` — список, `/<имя>/` — страница, `/compare/<имя>` — сравнение с эталоном,
`/reference/<имя>.png` — скриншот из указанного в `source.json` хранилища.
Портфолио переехало в `restored/portfolio/`, проверено: все пять адресов отвечают 200.

Заодно починил высоту iframe в сравнении — раньше была захардкожена под 4100px и
для макета другой высоты обрезала бы страницу. Теперь подгоняется по факту.

**Зачем.** Человек собирается выгрузить рабочий макет рядом с портфолио, и сравнивать
их надо независимо.
